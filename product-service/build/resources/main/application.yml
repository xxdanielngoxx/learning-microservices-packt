server.port: 7001
server.error.include-message: always

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root: INFO
    io.locngo.learning.microservices: DEBUG